# forza-painter-geometrize-diffbmp
Forza painter geometrizer with the DiffBMP algorithm.

Takes lots of GPU time, lots of VRAM and very confusing to tune (for average users). Only to produce results that are just not nearly as good as hill-climb methods.
